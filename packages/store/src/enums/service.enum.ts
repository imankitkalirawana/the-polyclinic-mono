export enum ServiceStatus {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}

export enum ServiceType {
  MEDICAL = "medical",
  SURGICAL = "surgical",
  DIAGNOSTIC = "diagnostic",
  CONSULTATION = "consultation",
}
