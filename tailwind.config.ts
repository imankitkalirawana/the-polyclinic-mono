import { pixelBasedPreset } from '@react-email/components';

export default {
  presets: [pixelBasedPreset],
  theme: {
    extend: {
      colors: {
        brand: '#007291',
      },
    },
  },
};
